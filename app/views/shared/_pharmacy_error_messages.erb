<% if @pharmacy.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      The form contains <%= pluralize(@pharmacy.errors.count, "error") %>.
    </div>
    <ul>
    <% @pharmacy.errors.full_messages.each do |msg| %>
      <li>* <%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>