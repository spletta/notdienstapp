<%= simple_form_for @emergency_group do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <p>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.association :pharmacies %>
  </p>
  <p><%= f.submit %></p>
<% end %>
